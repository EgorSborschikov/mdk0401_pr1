<Window x:Class="mdk0401_pr1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mdk0401_pr1"
        mc:Ignorable="d"
        FontFamily="Bahnschrift Light SemiCondensed"
        Title="Заявки партнеров" Height="450" Width="800"
        Icon="/Resources/NewTechnologies.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#BBDCFA">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10">
                <Image Width="45" Height="45" Source="/Resources/NewTechnologies.png"/>
                <TextBlock Text="Заявки партнеров" FontSize="18" FontWeight="Bold" 
                           VerticalAlignment="Center" Margin="10,0,0,0"/>
                <Button x:Name="BtnAdd" Content="Добавить заявку" Background="#0C4882" Foreground="White" Margin="20,0,0,0" Padding="10,5" Click="BtnAdd_Click"/>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ListBox x:Name="PartnersListBox" Margin="10" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Margin="0,5" Padding="10"
                                MouseLeftButtonUp="PartnerItem_MouseLeftButtonUp"
                                Cursor="Hand">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="White"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#F5F5F5"/>
                                            <Setter Property="BorderBrush" Value="#0C4882"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            
                            <StackPanel>
                                <TextBlock Text="{Binding Type}" FontWeight="Bold" FontSize="14"/>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                <TextBlock Text="{Binding Address}" Margin="0,5"/>
                                <TextBlock Text="{Binding Phone}" Margin="0,5"/>
                                <TextBlock Text="{Binding Rate, StringFormat='Рейтинг: {0}'}" Margin="0,5"/>

                                <TextBlock Text="Продукция:" FontWeight="Bold" Margin="0,10,0,5"/>

                                <ItemsControl ItemsSource="{Binding Products}" Margin="10,5">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding Name}"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Amount, StringFormat='{}Кол-во: {0}'}" Margin="10,0"/>
                                                <TextBlock Grid.Column="2" Text="{Binding Cost, StringFormat='{}Стоимость: {0:C}'}" Margin="10,0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <TextBlock Text="{Binding TotalCost, StringFormat='Общая стоимость заявки: {0:C}'}" 
                                           FontWeight="Bold" Margin="0,10,0,5" Foreground="DarkGreen"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
    </Grid>
</Window>
